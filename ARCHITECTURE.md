# البنية المعمارية

<div dir="rtl" align="right">

## الفكرة الأساسية

الإكستنشن **لا يحتوي على خادم لغة ولا مُنسِّق ولا مزوِّد إكمال**. كل ذلك
موجود أصلًا في إضافة `sumneko.lua` (Lua Language Server). ما يقدّمه هذا
المشروع هو **البيانات** فقط: ملفات تعريف مشروحة بصيغة LuaLS، وسطور قليلة
توجّه خادم اللغة إليها.

هذا الاختيار قلّص المشروع من «خادم لغة كامل» إلى ملف جافاسكربت واحد بطول
ثمانين سطرًا تقريبًا، مع الاحتفاظ بكل الميزات المطلوبة.

</div>

```
المستخدم يكتب Lua
        │
        ▼
┌───────────────────────┐        ┌──────────────────────────────┐
│  sumneko.lua (LuaLS)  │◄───────│  library/*.lua               │
│  إكمال · شرح · تنسيق  │        │  تعريفات ‎---@meta            │
└───────────────────────┘        └──────────────────────────────┘
        ▲                                       ▲
        │ Lua.workspace.library                 │ يولّدها
        │                                       │
┌───────────────────────┐        ┌──────────────────────────────┐
│  extension.js         │        │  tools/generate.js           │
│  يكتشف الفريمويرك     │        │  يسحب من مستودعات GitHub     │
│  ويضيف المسار         │        │  الرسمية ويستخرج التعريفات   │
└───────────────────────┘        └──────────────────────────────┘
```

<div dir="rtl" align="right">

## المكوّنات

### `tools/generate.js` — المولّد

يعمل وقت التطوير فقط، ولا يُشحن مع الحزمة. خطواته:

1. تنزيل أرشيف `tar.gz` لكل فريمويرك من `codeload.github.com`.
2. المرور على كل ملفات `.lua` مع تجاهل مجلدات الترجمة والإعدادات والواجهات.
3. لكل تعريف دالة، التقاط كتلة التعليقات `---` التي تعلوه مباشرة — وهي
   تحتوي أصلًا على وسوم `---@param` و `---@return` لأن الفريمويركات الثلاثة
   تكتب شروحها بصيغة LuaLS.
4. التقاط كتل `---@class` و `---@alias` و `---@enum` المستقلة كما هي.
5. توليد ملف `---@meta` لكل فريمويرك.

الأنماط الثلاثة التي يتعرّف عليها:

</div>

| النمط | مثال | يُستخدم في |
|---|---|---|
| `function A.B.C(...)` | `function ESX.GetPlayerFromId(source)` | ESX · QBCore |
| `function Class:Method(...)` | `function Player:AddMoney(type, amount)` | QBCore |
| `A.B = function(...)` | `ESX.Trace = function(msg)` | ESX |
| `function Name(...)` + `exports('X', Name)` | Qbox | Qbox |

<div dir="rtl" align="right">

### `library/*.lua` — التعريفات المولّدة

خمسة ملفات، كلها تبدأ بـ `---@meta` أي أن خادم اللغة يقرؤها كتعريفات لا
ككود يُنفَّذ:

</div>

| الملف | المحتوى |
|---|---|
| `qbcore.lua` | `QBCore.*` وصنف `QBCorePlayer` |
| `qbx.lua` | الأصناف والدوال العامة لـ Qbox |
| `qbx_exports.lua` | `exports.qbx_core:*` بصنف `QbxCoreExports` |
| `esx.lua` | `ESX.*` وأصناف `xPlayer` و `ESXJob` وغيرها |
| `cfx_manifest.lua` | توجيهات `fxmanifest.lua` — **مكتوب يدويًا**، يُحمَّل دائمًا |

<div dir="rtl" align="right">

### `extension.js` — التوصيل

عند التفعيل:

1. يقرأ الإعداد `fivemFrameworks.frameworks`.
2. إن كان `auto`، يبحث عن `qb-core` و `qbx_core` و `es_extended` في مساحة
   العمل، ويحمّل الكل إن لم يجد شيئًا.
3. يضيف مسارات ملفات التعريف إلى `Lua.workspace.library` بعد إزالة مسارات
   النسخ السابقة من الإكستنشن نفسه (المسار يحمل رقم الإصدار فيتغيّر مع
   كل تحديث).
4. يضبط `Lua.runtime.version` على `LuaJIT` لأنه المحرّك الذي يستخدمه فايف ام.

لا يكتب في الإعدادات إلا إذا تغيّرت القيمة فعلًا، تفاديًا لتعديل
`settings.json` عند كل فتح للمحرّر.

### `tools/check.js` — الحارس

يتحقّق أن كل ملف تعريف يبدأ بـ `---@meta`، وأن كل سطر دالة مكتمل البنية،
وألّا تتكرر التعريفات، وأن عدد الدوال فوق حدّ أدنى لكل فريمويرك. الحدّ
الأدنى هو الحماية الحقيقية: لو أعاد فريمويرك هيكلة كوده وتوقّفت التعبيرات
النمطية عن المطابقة، ينهار العدد ويفشل الفحص بدل أن تُنشر تعريفات فارغة.

## نقاط قرار مهمّة

</div>

| القرار | البديل المرفوض | السبب |
|---|---|---|
| الاعتماد على `sumneko.lua` | كتابة خادم لغة | آلاف الأسطر مقابل صفر |
| توليد التعريفات | كتابتها يدويًا | ١١٠٢ دالة من ٨ مستودعات، وتتقادم مع كل تحديث |
| اكتشاف تلقائي للفريمويرك | إعداد إجباري | أغلب المستخدمين لن يفتحوا الإعدادات |
| `X = {}` لا `X = X or {}` | — | الثاني يجعل النوع مفتوحًا فيُلغى فحص الحقول |
