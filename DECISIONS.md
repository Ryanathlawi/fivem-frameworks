# القرارات

<div dir="rtl" align="right">

سجلّ للقرارات التي غيّرت شكل المشروع، مع البديل المرفوض وسببه. الغرض ألّا
يُعاد نقاش محسوم بعد شهور.

</div>

---

## ‏١ · الاعتماد على `sumneko.lua` بدل كتابة خادم لغة

<div dir="rtl" align="right">

**القرار:** الإكستنشن يشحن بيانات فقط، ويعلن `extensionDependencies` على
`sumneko.lua` ليتولّى الإكمال والشرح والتواقيع والانتقال والتنسيق.

**البديل المرفوض:** تنفيذ `CompletionItemProvider` و `HoverProvider` و
`DocumentFormattingEditProvider` داخل الإكستنشن.

**السبب:** البديل آلاف الأسطر ونتيجته أضعف — لن يفهم أنواع المتغيّرات ولا
يتتبّع `local Player = QBCore.Functions.GetPlayer(src)` ليعرف ما الذي يكمله
بعد النقطة. خادم اللغة يفعل ذلك أصلًا متى ما أعطيته تعريفات.

</div>

---

## ‏٢ · توليد التعريفات من المصدر بدل كتابتها يدويًا

<div dir="rtl" align="right">

**القرار:** `tools/generate.js` ينزّل مستودعات الفريمويركات الرسمية ويستخرج
منها التعريفات.

**البديل المرفوض:** كتابة تواقيع الدوال بخط اليد.

**السبب:** الفريمويركات الثلاثة تكتب شروحها أصلًا بصيغة LuaLS
(`---@param` و `---@return`)، فالكتابة اليدوية إعادة طباعة لأكثر من ألف دالة،
ثم تتقادم مع أول تحديث. التوليد يجعل التحديث أمرًا واحدًا.

</div>

---

## ‏٣ · اكتشاف الفريمويرك تلقائيًا

<div dir="rtl" align="right">

**القرار:** البحث عن `qb-core` و `qbx_core` و `es_extended` في مساحة
العمل، وتحميل الكل عند عدم العثور على شيء.

**البديل المرفوض:** إعداد إجباري يختار منه المستخدم.

**السبب:** من يركّب إكستنشن لا يفتح إعداداته. والسقوط إلى «حمّل الكل» أفضل
من إكستنشن يبدو معطّلًا. الإعداد موجود لمن أراد التحكّم، لا كشرط للتشغيل.

</div>

---

## ‏٤ · `X = {}` لا `X = X or {}` في ملفات التعريف

<div dir="rtl" align="right">

**القرار:** إعلان الجداول بإسناد مباشر.

**السبب:** `X or {}` ينتج نوعًا اتحاديًا مع `table`، وهو نوع **مفتوح**،
فيتوقف خادم اللغة عن التحقق من الحقول. اكتُشف عمليًا: بعد التغيير بدأ
`missing-parameter` و `need-check-nil` يظهران على استدعاءات خاطئة حقيقية.

</div>

---

## ‏٥ · إعادة توجيه نوع الإرجاع في دوال QBCore

<div dir="rtl" align="right">

**القرار:** استبدال `---@return table` بـ `---@return QBCorePlayer` في
`GetPlayer` وأخواتها، وتوليد صنف `QBCorePlayer` تُنشر عليه دوال اللاعب
بصيغة `Player.Functions.X` لا `Player:X`.

**السبب:** QBCore يعرّف الدوال بصيغة `function Player:AddMoney(...)` لكن
الاستخدام الفعلي `Player.Functions.AddMoney(...)`، وتوثيقه يقول إن
`GetPlayer` ترجع `table` مجرّدًا. بلا هذا التعديل ينتهي الإكمال عند النقطة
الأولى — وهذا أكثر أنماط QBCore استعمالًا.

</div>

---

## ‏٦ · README ثنائي اللغة

<div dir="rtl" align="right">

**القرار:** الجسم الأساسي بالعامية السعودية كما هو عُرف مستودعات المالك،
مع قسم إنجليزي مختصر في آخره.

**السبب:** `README.md` هو صفحة الإكستنشن على Marketplace ويقرؤها العالم
كله. الاكتفاء بالعربية يقلّص الوصول، والاكتفاء بالإنجليزية يخالف عُرف
المالك. القسمان يحلّان الاثنين.

**قابل للمراجعة:** لو فضّل المالك عربية خالصة، القرار قراره.

</div>

---

## ‏٧ · لا snippets

<div dir="rtl" align="right">

**القرار:** عدم شحن ملفات snippets.

**السبب:** الإكمال المبني على الأنواع يعرف السياق — يقترح دوال السيرفر في
ملف السيرفر ودوال اللاعب بعد `Player.Functions.` — بينما snippets تقترح كل
شيء في كل مكان. إضافتها كانت ستزيد الضجيج لا الفائدة.

</div>

---

## ‏٨ · إسقاط دعم vRP

<div dir="rtl" align="right">

**القرار:** إزالة vRP من الإكستنشن نهائيًا (٢٠٢٦-٠٩-٠١، بطلب المالك).

**ما أُزيل:** سطره من `FRAMEWORKS` في المولّد، وملف `library/vrp.lua`
(‏٤٧٥ دالة)، ومدخله في `DETECT` و `FILES`، وخياره من `enum` الإعداد.

**الأثر:** التغطية من ‏١٢٠٦ دالة إلى ‏١١٠٢، والفريمويركات من أربعة إلى ثلاثة.

**كيف يُعاد:** إضافة سطر واحد إلى `FRAMEWORKS` في `tools/generate.js`:

</div>

```js
{ id: 'vrp', repo: 'vRP-framework/vRP', branch: 'master', title: 'vRP' },
```

<div dir="rtl" align="right">

ثم مدخل في `DETECT` و `FILES` داخل `extension.js`، وحدّ أدنى في `check.js`،
وخيار في `enum` داخل `package.json`. المولّد نفسه لم يُمَس — أنماط
`function Class:Method(...)` التي يستعملها vRP ما زالت مدعومة.

</div>

---

## ‏٩ · لا تُصدَّر إلا الجداول العامة فعلًا

<div dir="rtl" align="right">

**القرار:** الدالة المعرّفة على جدول أعلنه الفريمويرك `local` لا تُكتب في
ملف التعريف، إلا إذا أعاد الملف نفسه كشفه عبر `_ENV.x = x` أو `_G.x = x`.

**المشكلة التي كشفته:** كان المولّد يرى `function Player:AddMoney(...)`
فيفترض أن `Player` جدول عام، بينما هو `local Player = {}` داخلي في
`qb-core`. النتيجة أن الإكمال كان يعرض جداول الفريمويرك الخاصة، وأخطر
منها أنه أعلن `string = {}` و `math = {}` و `package = {}` — وهذه
**مكتبات Lua القياسية**، فإعلانها يستبدلها ويخفي `string.format` وأخواتها.

**الاستثناء `_ENV`:** Qbox يكتب `local qbx = {}` ثم `_ENV.qbx = qbx` في
آخر الملف، فـ `qbx` عام فعلًا. القاعدة تفرّق بين الحالتين بدقّة.

**ما رُفض:** إضافة أسماء الباراميترات إلى قائمة الخاص. جُرّبت فابتلعت
`qbx` كاملًا (‏١٦٤ دالة إلى ‏١٥)، وأمسكها `check.js`. الفائدة كانت اسمًا
واحدًا مكرّرًا مقابل خسارة فريمويرك كامل.

**ما رُفض أيضًا:** اعتبار `return x` كشفًا للجدول. `return self` في آخر
أي بانٍ نمط Lua شائع ولا يقول شيئًا عن السطح العام.

**الأثر:** السطح العام لـ Qbox من ‏١٦٤ إلى ‏٤٧ في `qbx.lua` — والباقي لم
يضع، هو أصلًا في `qbx_exports.lua` لأن Qbox يُستهلك بالتصديرات. وحدود
`check.js` عُدِّلت لتعكس السطح العام لا المصفّى، مع بقاء قدرتها على مسك
انهيار الاستخراج (‏٤٧ تصير صفرًا لو انكسر النمط).

</div>

---

## ‏١٠ · تعريف سطر الإقلاع (`GetCoreObject` و `getSharedObject`)

<div dir="rtl" align="right">

**القرار:** كتابة صنف تصديرات يدويًا لـ `qb-core` و `es_extended` يعطي
`GetCoreObject()` نوع `QBCore` و `getSharedObject()` نوع `ESX`.

**المشكلة:** الإكستنشن كان يعمل في التجارب ولا يعمل في السكربتات الحقيقية.
كل سكربت QBCore في الدنيا يبدأ بـ:

</div>

```lua
local QBCore = exports['qb-core']:GetCoreObject()
```

<div dir="rtl" align="right">

وكلمة `local` هنا **تحجب** الجدول العام الذي نعلنه، فيصير `QBCore`
متغيّرًا مجهول النوع، ويموت الإكمال عند أول نقطة — في الشكل الذي يكتب به
الناس فعلًا، لا في تجاربنا.

**كيف اكتُشف:** باختبار السطر الحقيقي بدل السطر المريح. ESX نجح لأن كثيرين
يكتبونه عامًّا (`ESX = exports[...]`) فيندمج مع إعلاننا، وQBCore فشل.

**خطأ ثانٍ ظهر أثناء الإصلاح:** `render()` كانت تتحقق من إعلان الصنف
بالبادئة، و `---@class ESXAccount` تبدأ بـ `---@class ESX`، فظنّت أن `ESX`
معرّف ولم تعلنه. النتيجة أن `ESX` بقي جدولًا بلا نوع وكل `ESX.*` مجهول.
صارت المطابقة بالاسم الكامل.

**الدرس:** اختبر النمط الذي يكتبه المستخدم، لا النمط الذي يسهل اختباره.

</div>

---

## ‏١١ · شحن الـ natives ضمن الحزمة

<div dir="rtl" align="right">

**القرار:** إضافة `overextended.cfxlua-vscode` إلى `extensionPack`، فيُركَّب
تلقائيًا مع الإكستنشن.

**المشكلة:** الإكستنشن يغطّي الفريمويركات لا الـ natives. المستخدم يركّبه،
يفتح ملف كلاينت، يكتب `GetEntityCoords` ويرى خطًا أحمر — فيستنتج أن
الإكستنشن معطّل ويزيله. الثغرة ليست تقنية بل في أول انطباع.

**لماذا `extensionPack` لا `extensionDependencies`:** الاعتمادية الصلبة
تمنع المستخدم من إزالة الـ natives ما دام إكستنشننا مركّبًا، وهذا ظلم
لشيء **مكمّل** لا لازم — الإكمال يعمل تمامًا بدونه. `sumneko.lua` وحده
يبقى اعتمادية صلبة لأنه المحرّك فعلًا.

**تُحقّق قبل الإضافة:** أن `overextended.cfxlua-vscode` موجود على
Marketplace (رمز ‏٢٠٠). معرّف خاطئ هنا يجعل الإكستنشن **غير قابل للتركيب**
عند الجميع.

**النتيجة المختبَرة:** ملف واحد فيه `PlayerPedId()` و `GetEntityCoords()`
و `QBCore.Functions.GetPlayer()` يمرّ بلا أي تحذير.

</div>

---

## ‏١٢ · توسيع التغطية إلى الموارد لا الفريمويركات وحدها

<div dir="rtl" align="right">

**القرار:** إضافة `ox_lib` و `ox_inventory` و `qb-target` و `qb-menu` و
`qb-inventory` إلى نفس آلية التوليد والاكتشاف.

**السبب:** جزء كبير من أي سكربت حقيقي ليس نداءات الفريمويرك، بل هذه
الموارد. و `ox_lib` وحده أغنى توثيقًا من الفريمويركات الثلاثة مجتمعة
(‏٢٥٧ دالة و ‏٨٧ صنفًا).

**ما تطلّبه:** تعميم آلية التصديرات التي كانت خاصة بـ Qbox، وثلاثة إصلاحات
كشفها الاختبار الحقيقي:

</div>

| العطل | الأثر قبل الإصلاح |
|---|---|
| `exports('x', function(...))` المباشرة لا تُلتقط | ox_inventory بلا تصديرات |
| `local function` لا تُسجَّل مرجعًا للتصدير | qb-menu بصفر تصديرات |
| مرجع منقّط `exports('Search', Inventory.Search)` | ‏`Search` مفقودة |

<div dir="rtl" align="right">

**عطل رابع أدقّ:** `ox_lib` يحتفظ بنسخة **مهجورة** من `lib.notify` على
السيرفر بتوقيع مختلف، وهي أكثر توثيقًا من النسخة الحالية — فقاعدة «الأكثر
توثيقًا يفوز» كانت تشحن التوقيع الميت. صارت القاعدة: غير المهجور يفوز أولًا.

**ما استُبعد:** `ox_target` — يسجّل تصديراته داخل حلقة `exports(index, value)`
فلا وجود لأسمائها في الكود الساكن. استخراجها يحتاج تحليل جداول، وهو عمل
كثير لمورد واحد.

**ما استُبعد أيضًا:** كتابة ملف تعريف لأي مورد ينتج ملفًا فارغًا — الملف
الفارغ ضجيج في قائمة الإكمال لا تغطية.

</div>
