# القرارات

<div dir="rtl" align="right">

سجلّ للقرارات التي غيّرت شكل المشروع، مع البديل المرفوض وسببه. الغرض ألّا
يُعاد نقاش محسوم بعد شهور.

</div>

---

## ‏١ · الاعتماد على `sumneko.lua` بدل كتابة خادم لغة

<div dir="rtl" align="right">

**القرار:** الإكستنشن يشحن بيانات فقط، ويعلن `extensionDependencies` على
`sumneko.lua` ليتولّى الإكمال والشرح والتواقيع والانتقال والتنسيق.

**البديل المرفوض:** تنفيذ `CompletionItemProvider` و `HoverProvider` و
`DocumentFormattingEditProvider` داخل الإكستنشن.

**السبب:** البديل آلاف الأسطر ونتيجته أضعف — لن يفهم أنواع المتغيّرات ولا
يتتبّع `local Player = QBCore.Functions.GetPlayer(src)` ليعرف ما الذي يكمله
بعد النقطة. خادم اللغة يفعل ذلك أصلًا متى ما أعطيته تعريفات.

</div>

---

## ‏٢ · توليد التعريفات من المصدر بدل كتابتها يدويًا

<div dir="rtl" align="right">

**القرار:** `tools/generate.js` ينزّل مستودعات الفريمويركات الرسمية ويستخرج
منها التعريفات.

**البديل المرفوض:** كتابة تواقيع الدوال بخط اليد.

**السبب:** الفريمويركات الثلاثة تكتب شروحها أصلًا بصيغة LuaLS
(`---@param` و `---@return`)، فالكتابة اليدوية إعادة طباعة لسبعمئة دالة،
ثم تتقادم مع أول تحديث. التوليد يجعل التحديث أمرًا واحدًا.

</div>

---

## ‏٣ · اكتشاف الفريمويرك تلقائيًا

<div dir="rtl" align="right">

**القرار:** البحث عن `qb-core` و `qbx_core` و `es_extended` في مساحة
العمل، وتحميل الكل عند عدم العثور على شيء.

**البديل المرفوض:** إعداد إجباري يختار منه المستخدم.

**السبب:** من يركّب إكستنشن لا يفتح إعداداته. والسقوط إلى «حمّل الكل» أفضل
من إكستنشن يبدو معطّلًا. الإعداد موجود لمن أراد التحكّم، لا كشرط للتشغيل.

</div>

---

## ‏٤ · `X = {}` لا `X = X or {}` في ملفات التعريف

<div dir="rtl" align="right">

**القرار:** إعلان الجداول بإسناد مباشر.

**السبب:** `X or {}` ينتج نوعًا اتحاديًا مع `table`، وهو نوع **مفتوح**،
فيتوقف خادم اللغة عن التحقق من الحقول. اكتُشف عمليًا: بعد التغيير بدأ
`missing-parameter` و `need-check-nil` يظهران على استدعاءات خاطئة حقيقية.

</div>

---

## ‏٥ · إعادة توجيه نوع الإرجاع في دوال QBCore

<div dir="rtl" align="right">

**القرار:** استبدال `---@return table` بـ `---@return QBCorePlayer` في
`GetPlayer` وأخواتها، وتوليد صنف `QBCorePlayer` تُنشر عليه دوال اللاعب
بصيغة `Player.Functions.X` لا `Player:X`.

**السبب:** QBCore يعرّف الدوال بصيغة `function Player:AddMoney(...)` لكن
الاستخدام الفعلي `Player.Functions.AddMoney(...)`، وتوثيقه يقول إن
`GetPlayer` ترجع `table` مجرّدًا. بلا هذا التعديل ينتهي الإكمال عند النقطة
الأولى — وهذا أكثر أنماط QBCore استعمالًا.

</div>

---

## ‏٦ · README ثنائي اللغة

<div dir="rtl" align="right">

**القرار:** الجسم الأساسي بالعامية السعودية كما هو عُرف مستودعات المالك،
مع قسم إنجليزي مختصر في آخره.

**السبب:** `README.md` هو صفحة الإكستنشن على Marketplace ويقرؤها العالم
كله. الاكتفاء بالعربية يقلّص الوصول، والاكتفاء بالإنجليزية يخالف عُرف
المالك. القسمان يحلّان الاثنين.

**قابل للمراجعة:** لو فضّل المالك عربية خالصة، القرار قراره.

</div>

---

## ‏٧ · لا snippets

<div dir="rtl" align="right">

**القرار:** عدم شحن ملفات snippets.

**السبب:** الإكمال المبني على الأنواع يعرف السياق — يقترح دوال السيرفر في
ملف السيرفر ودوال اللاعب بعد `Player.Functions.` — بينما snippets تقترح كل
شيء في كل مكان. إضافتها كانت ستزيد الضجيج لا الفائدة.

</div>

---

## ‏٨ · إسقاط دعم vRP

<div dir="rtl" align="right">

**القرار:** إزالة vRP من الإكستنشن نهائيًا (٢٠٢٦-٠٩-٠١، بطلب المالك).

**ما أُزيل:** سطره من `FRAMEWORKS` في المولّد، وملف `library/vrp.lua`
(‏٤٧٥ دالة)، ومدخله في `DETECT` و `FILES`، وخياره من `enum` الإعداد.

**الأثر:** التغطية من ‏١٢٠٦ دالة إلى ‏٧٣١، والفريمويركات من أربعة إلى ثلاثة.

**كيف يُعاد:** إضافة سطر واحد إلى `FRAMEWORKS` في `tools/generate.js`:

</div>

```js
{ id: 'vrp', repo: 'vRP-framework/vRP', branch: 'master', title: 'vRP' },
```

<div dir="rtl" align="right">

ثم مدخل في `DETECT` و `FILES` داخل `extension.js`، وحدّ أدنى في `check.js`،
وخيار في `enum` داخل `package.json`. المولّد نفسه لم يُمَس — أنماط
`function Class:Method(...)` التي يستعملها vRP ما زالت مدعومة.

</div>
