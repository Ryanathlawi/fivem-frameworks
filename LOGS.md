# سجلّ العمل

<div dir="rtl" align="right">

## ‏٢٠٢٦-٠٩-٠١ — من الفكرة إلى حزمة جاهزة

### الاستطلاع

فُحصت مصادر الفريمويركات الثلاثة قبل كتابة أي سطر. الاكتشاف الذي غيّر
شكل المشروع كله: **الفريمويركات الثلاثة تكتب شروحها أصلًا بصيغة LuaLS**.

</div>

| الفريمويرك | ما وُجد |
|---|---|
| ESX | `---@param` و `---@return` كاملة، وصنف `StaticPlayer` موصوف بالكامل |
| QBCore | شروح كاملة في `server/functions.lua` |
| Qbox | ملف `types.lua` يبدأ بـ `---@meta` — صالح للشحن كما هو |

<div dir="rtl" align="right">

نتيجة ذلك: المشروع صار **حزمة بيانات** لا خادم لغة. لا داعي لكتابة مزوّد
إكمال ولا مُنسِّق ما دام `sumneko.lua` يفعلهما متى ما أُعطي تعريفات.

ملاحظة جانبية: مستودع QBCore انتقل من `qbcore-framework/qb-core` إلى
`qbcore-fivem/qb-core` — الرابط القديم يعيد التوجيه بـ ‏301.

### البناء

</div>

| الملف | الدور |
|---|---|
| `tools/generate.js` | تنزيل واستخراج وتوليد |
| `extension.js` | اكتشاف الفريمويرك وربط `Lua.workspace.library` |
| `tools/check.js` | حارس ضدّ انهيار التوليد |
| `tools/icon.js` | توليد `icon.png` عبر `zlib` وحدها |

<div dir="rtl" align="right">

### ثلاث مشكلات حقيقية ظهرت أثناء العمل

**‏١ · `tar` يفشل بـ `status: 2` بلا رسالة**

نسخة GNU tar المرفقة مع Git for Windows تقرأ `C:\Users\...` على أنه
`host:path` أي أرشيف على جهاز بعيد. النقطتان في `C:` هي السبب.
الحل: تشغيل `tar` بـ `cwd` مضبوط ومسارات نسبية.

**‏٢ · تعريفات مكرّرة في تصديرات Qbox**

`tools/check.js` أمسكها في أول تشغيل: ‏٧٢ اسمًا فريدًا مقابل ‏٨٥ تعريفًا.
السبب أن Qbox يسجّل نفس اسم التصدير على السيرفر والكلاينت. الحل: فهرسة
الدوال العامة بـ `side:name`، ووسم ما تكرّر على الجهتين بـ `[client/server]`.

هذه هي الفائدة التي دفعها الحارس ثمنه في أول دقيقة من وجوده.

**‏٣ · `X = X or {}` يُلغي فحص الأنواع**

الاتحاد مع `table` ينتج نوعًا **مفتوحًا** فيتوقّف خادم اللغة عن التحقق من
الحقول. بعد التحويل إلى `X = {}` بدأت التشخيصات الحقيقية تظهر.

### تعديل خاص بـ QBCore

QBCore يعرّف دوال اللاعب بصيغة `function Player:AddMoney(...)` بينما
الاستخدام الفعلي `Player.Functions.AddMoney(...)`، وتوثيقه يقول إن
`GetPlayer` ترجع `table` مجرّدًا. بلا تدخّل، ينتهي الإكمال عند النقطة
الأولى — وهذا أكثر أنماط QBCore استعمالًا.

الحل: توليد صنف `QBCorePlayer` تُنشر عليه الدوال بالصيغة الصحيحة، وإعادة
توجيه نوع الإرجاع في `GetPlayer` وأخواتها إليه.

### التحقق من طرف إلى طرف

بدل الاكتفاء بالتخمين، شُغّل خادم لغة Lua نفسه بوضع `--check` على مجلد
اختبار يستدعي الفريمويركات الثلاثة:

</div>

| النتيجة | ما أثبتته |
|---|---|
| `missing-parameter` على `QBCore.Functions.Notify` | تواقيع QBCore محمّلة ومُحترَمة |
| `need-check-nil` على `ESX.GetPlayerFromId` | ‏`@return xPlayer?` يعمل |
| مطابقة `NotificationType` عبر `exports.qbx_core:Notify` | الأصناف والـ aliases تُحَل عبر جدول التصديرات |
| لا تشخيص على `Player.Functions.AddMoney` | صنف `QBCorePlayer` موصول صحيحًا |

<div dir="rtl" align="right">

### الأرقام النهائية

</div>

| القياس | القيمة |
|---|---|
| الدوال المغطّاة | ‏١١٠٢ |
| الأصناف | ‏٢٠٣ |
| المصادر | ‏٨ مستودعات |
| حجم الحزمة | ‏٦٣٫٦ كيلوبايت |
| ملفات تُشحن | ‏١٩ |
| سطور جافاسكربت وقت التشغيل | ‏~٨٠ |

<div dir="rtl" align="right">

### ما بقي

النشر على Marketplace. يحتاج حساب Azure DevOps وتوكن `Marketplace → Manage`
وناشرًا مسجّلًا — كلها بيد المالك. راجع `QUESTIONS.md`.

</div>
